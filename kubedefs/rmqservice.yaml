apiVersion: v1
kind: Service
metadata:
  name: vpromq01
spec:
  selector:
    app: vpromq
  ports:
    - name: vprormq-port
      port: 5672
      targetPort: 5672  
      protocol: TCP 
  type: ClusterIP
apiVersion: v1
kind: service
metadata:
  name: vprodb
  labels:
    app: vprodb
spec:
  selector:
    app: vprodb 
  ports:
    - name: vprodb-port
      port: 3306
      targetPort: 3306  
      protocol: TCP
  type: ClusterIP 
